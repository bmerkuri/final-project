table:
  name: users
  schema: public
array_relationships:
  - name: boards_admins
    using:
      foreign_key_constraint_on:
        column: user_id
        table:
          name: boards_admins
          schema: public
